---
import { cn } from "@lib/utils";

type Props = {
  href: string;
  class?: string;
  [key: string]: any;
};

const { href, class: className, ...props }: Props = Astro.props;
---

<a href={href} class={cn(className)} {...props}>
  <slot />
</a>

