<!-- ---
const {
  title = 'Items',
  items = [], // Expect array of { name, icon?, tooltip? }
  className = '',
} = Astro.props
---

<section class={`my-6 ${className}`}>
  <h2 class="text-lg font-semibold mb-3">{title}</h2>

  <ul class="flex flex-wrap gap-3 print:mt-4">
    {
      items.length > 0 ? (
        items.map(({ name, icon, tooltip }) => (
          <li
            class="flex items-center gap-2 rounded-md border border-gray-300 bg-black-100 px-2 py-1 text-sm text-gray-800 hover:bg-gray-200 transition-colors print:border-none print:bg-transparent"
            title={tooltip || name}
          >
            {icon && <span class={`icon-[${icon}] size-4`} />}
            <span>{name}</span>
          </li>
        ))
      ) : (
        <li class="text-gray-500 italic text-sm">No items available</li>
      )
    }
  </ul>
</section> -->
